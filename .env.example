# Local development defaults for the Docker MariaDB stack.
# Copy backend/.env.local.example to backend/.env.local for the app runtime.

COMPOSE_PROJECT_NAME=bilal-rms
LOCAL_DB_HOST=127.0.0.1
LOCAL_DB_PORT=3307
LOCAL_DB_NAME=bilal_rms
LOCAL_DB_USER=bilal_rms
LOCAL_DB_PASSWORD=bilal_rms
